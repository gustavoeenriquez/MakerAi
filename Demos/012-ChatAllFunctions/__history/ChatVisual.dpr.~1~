program ChatVisual;

uses
  System.StartUpCopy,
  FMX.Forms,
  FMX.Skia,
  uMainChatVisual in 'uMainChatVisual.pas' {Form6},
  Unit2 in 'Unit2.pas' {Form2},
  Unit3 in 'Unit3.pas' {Form3},
  uAddRagData in 'uAddRagData.pas' {Form4};

{$R *.res}

begin
  GlobalUseSkia := True;
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm4, Form4);
  Application.Run;
end.
